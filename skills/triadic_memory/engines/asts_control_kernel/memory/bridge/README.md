# Memory Bridge (Canonical Source)

This directory is the authoritative implementation of the Triadic Memory Crucible bridge as it ships inside ASTS. If you modify `openclaw_workspace.py` or related files, re-sync `scripts/asts_memory` and `engines/asts_memory_bridge` so the skill snapshots stay in lockstep.
