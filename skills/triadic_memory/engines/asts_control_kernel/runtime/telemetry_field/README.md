# Telemetry Field
This directory defines telemetry aggregation and validation helpers.

## What it does
- Merges observer outputs and computes canonical telemetry envelope (`theta`).

## Mini directory
- `aggregator.py`
- `normalization.py`
- `contracts.py`

## Notes
- Keep aggregation deterministic and contract changes versioned.
