import requests

def safe_scrape(url, proxy=None):
    proxies = {"http":proxy,"https":proxy} if proxy else None
    r = requests.get(url, proxies=proxies, timeout=20)
    r.raise_for_status()
    return r.text